/* Entendendo os tipos de Dados Primitivos */

/* string */
const nome = 'Vinicius'; 
const sobreNome = "Mattos";
const ultimoNome = `Silva`;

/* number */
const idade = 32;
const altura = 1.86;

/* Undefined = não aponta para local nenhuma na memoria. */
let nomeAluno;

/* Null = não aponta para local nenhuma na memoria. */
const sobreNomeAluno = null;

/* Boolean = true false (lógico)*/
const aprovado = true;